metadata:
  name: "Kali Linux 2023.4"
  type: "workstation"
  os_family: "linux"
  role: "offensive"  # ← Metadado extra!

emulation:
  services:
    - { port: 22, name: "ssh" }
  
  file_tree:
    /root:
      - ".bash_history"  # ← Pode ter comandos de ataque!
    /root/tools:
      - "exploit.py"
      - "payload.sh"